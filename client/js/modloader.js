// This file will contain stuff for loading mods and apis for mods to interact with the game.

moddingEnabled = false;

function loadMod(url) {
    if (moddingEnabled) {
        //load the mod at url
    } else {
        return false;
    }
}