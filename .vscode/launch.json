{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "edge",
            "request": "launch",
            "name": "Launch Edge with Debugger",
            "version": "canary",
            "file": "${workspaceFolder}/client/index.html"
        }
    ]
}